version: '3'

services:
  web:
    container_name: glam-client
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - 3000:80
    environment:
      - REACT_APP_API_URL=http://127.0.0.1:8000